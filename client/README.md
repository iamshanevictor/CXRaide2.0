# CXRaide Frontend

Vue 3 + Vite frontend for CXRaide 2.0.

```powershell
npm install
$env:VITE_API_BASE_URL="http://localhost:5000"
npm run dev
```

Build for Vercel:

```powershell
npm run build
```

Output directory: `dist`.
